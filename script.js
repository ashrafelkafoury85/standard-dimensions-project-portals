const menuBtn=document.getElementById('menuBtn');const nav=document.getElementById('mainNav');menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const search=document.getElementById('searchInput'),project=document.getElementById('projectFilter'),locationFilter=document.getElementById('locationFilter'),statusFilter=document.getElementById('statusFilter'),cards=[...document.querySelectorAll('.project-card')],empty=document.getElementById('emptyState');
function filter(){const q=search.value.trim().toLowerCase(),p=project.value.toLowerCase(),l=locationFilter.value,s=statusFilter.value;let shown=0;cards.forEach(c=>{const hay=(c.dataset.name+' '+c.textContent).toLowerCase();const okQ=!q||hay.includes(q);const okP=p==='all'||c.dataset.name===p;const okL=l==='all'||c.dataset.location===l;const okS=s==='all'||c.dataset.status===s;const ok=okQ&&okP&&okL&&okS;c.classList.toggle('hidden',!ok);if(ok)shown++});empty.hidden=shown!==0}
document.getElementById('filterForm').addEventListener('submit',e=>{e.preventDefault();filter()});[search,project,locationFilter,statusFilter].forEach(el=>el.addEventListener(el===search?'input':'change',filter));document.getElementById('focusSearch').addEventListener('click',()=>{document.getElementById('projects').scrollIntoView({behavior:'smooth'});setTimeout(()=>search.focus(),500)});document.getElementById('year').textContent=new Date().getFullYear();
